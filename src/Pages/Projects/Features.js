import React from 'react';

const Features = ({feature}) => {
    return (
        <div>
            
            <li className='text-lg text-white'>{feature}</li>
        </div>
    );
};

export default Features;